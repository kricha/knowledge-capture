interface:
  display_name: "Knowledge Capture"
  short_description: "Save local raw repo notes"
  default_prompt: "Use $knowledge-capture after meaningful repo work to save what changed, what was learned, what was decided, and what is next."
